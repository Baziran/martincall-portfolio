"""Production ML primitives shared by MartinCall runtime modules.

Add reusable model inference, feature normalization, and common ML helpers here,
not in an in-repository prototype package. Experimental ML code and artifacts
belong below ``../data/`` and are never runtime imports.
"""
