    const GEX_CONTEXT_POLL_MS = 60000;
    const GEX_LIVE_CAPTURE_REVISION_MS = 5 * 60 * 1000;
    const GEX_MOTION_TTL_MS = 25000;
    const GEX_MOTION_FRAME_MS = 320;
    const GEX_STREAM_STALE_MS = 20000;
    const GEX_HISTORY_VIEW_MODES = Object.freeze(["last", "1d", "2d"]);
    const GEX_DISPLAY_LEVEL_COUNTS = Object.freeze([11, 15, 21]);
    const GEX_DEFAULT_DISPLAY_LEVEL_COUNT = 21;
